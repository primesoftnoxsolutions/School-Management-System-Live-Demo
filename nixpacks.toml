# Install from monorepo root so workspaces + package-lock stay in sync.
providers = ["node"]

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["echo Backend workspace ready"]

[start]
cmd = "npm run start"
