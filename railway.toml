[build]
builder = "NIXPACKS"
# Install phase already runs npm ci — do NOT run it again (EBUSY on node_modules/.cache mount).
buildCommand = "npm run build"

[deploy]
startCommand = "npm run start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
